#include <stdio.h>

int main(void)
{
	int max;
	int a = 3, b = 5;

	max = a>b ? a : b;
	printf("max = %d\n", max);

	return 0;
}

/**************************************************************************
时间：2015年4月19日20:46:10
目的：练习三目运算符
功能：输出两个数的较大者
其他：A ? B : C; <=> if (A) B; else C;即如果A成立则输出B，否则输出C
在VC中的输出结果为：
---------------------------------------------------------------------------
max = 5
Press any key to continue
---------------------------------------------------------------------------
**************************************************************************/